# Jarvis AI Systems v2 — optional dependencies
# pip install -r requirements-systems.txt
# Core Jarvis still runs without these; each module degrades gracefully.

# Orchestration / agents
langgraph>=0.2.0
langchain-core>=0.3.0
openai-agents>=0.0.14
agno>=1.0.0
pydantic-ai>=0.0.20
cursor-sdk>=0.1.0

# RAG
llama-index>=0.12.0
llama-index-core>=0.12.0

# Scrape / HTML
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Verify
pytest>=8.0.0
